name: Build Windows EXE with Nuitka

on:
  workflow_dispatch:
  push:
    paths:
      - 'minio_server.py'
      - 'main.py'
      - 'templates/**'
      - 'requirements.txt'
      - 'installer.iss'
      - '.github/workflows/**'

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 90

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python 3.8 (32-bit)
        uses: actions/setup-python@v5
        with:
          python-version: '3.8'
          architecture: 'x86'

      - name: Verify Python environment
        run: |
          python --version
          python -c "import struct; print('Architecture:', struct.calcsize('P') * 8, 'bit')"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt

      - name: Install Nuitka
        run: |
          pip install "nuitka[full]"

      - name: Build EXE with Nuitka
        shell: cmd
        run: |
          call "C:\Program Files (x86)\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars32.bat"
          nuitka --standalone --follow-import-to=minio_server --include-package=minio --include-package=flask --include-package=flask_cors --include-package=pywebview --include-package=pythonnet --include-data-dir=templates=templates --include-data-dir=assets=assets --windows-icon-from-ico=assets/app.ico --windows-company-name=MinIOFileService --windows-product-name="MinIO File Service" --windows-file-description="MinIO File Management" --windows-product-version=1.0.0 --windows-file-version=1.0.0 --nofollow-import-to=unittest --nofollow-import-to=pytest --nofollow-import-to=pydoc_data --output-dir=dist/minio_file_service --output-filename=minio_file_service main.py

      - name: Rename exe if needed
        shell: pwsh
        run: |
          if (Test-Path "dist\minio_file_service\main.exe") {
            Rename-Item "dist\minio_file_service\main.exe" "minio_file_service.exe" -Force
          }
          Get-ChildItem "dist\minio_file_service" -Filter "*.exe" | ForEach-Object { Write-Host $_.Name }

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y

      - name: Compile installer with Inno Setup
        shell: pwsh
        run: |
          $iscc = Get-ChildItem "C:\Program Files (x86)\Inno Setup*\ISCC.exe" | Select-Object -First 1
          if (-not $iscc) { Write-Error "ISCC.exe not found"; exit 1 }
          Write-Host "Using ISCC: $($iscc.FullName)"
          & $iscc.FullName installer.iss
          if (-not $?) { exit 1 }

      - name: Upload standalone folder artifact
        uses: actions/upload-artifact@v4
        with:
          name: minio-file-service-standalone
          path: dist/minio_file_service/
          retention-days: 30

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: minio-file-service-installer
          path: installer/
          retention-days: 30